# 一起维护 · Contributing

先说句实在话：这个工具有一部分依赖对 Get笔记接口的**逆向**（比如录音原文、发芽）。
官方哪天一改，某个功能就可能失效。**与其一个人追着修，不如我们一起。** 欢迎你加入。

## 你可以怎么帮忙（从易到难）
- 🐛 **报告失效**：哪个脚本/接口不工作了，开个 issue，贴上报错和你的环境，就帮了大忙。
- 🔧 **修接口**：接口变了？`docs/API.md` 记录了现有端点与逆向方法，按里面的思路抓包对照、改 `scripts/` 即可。
- 🧩 **分享你的配置**：你的标签库 / 分类 / 路由（`config/*.yaml`）打磨得不错，欢迎贡献成新的 `*.example` 供别人借鉴。
- 📝 **改文档**：哪一步说得不够清楚、你卡过的地方，帮忙补一句。
- 🌍 **翻译**：英文版 README 很受欢迎。

## 提交方式
1. Fork → 建分支 → 改动 → PR。PR 里说清「改了什么、为什么、怎么验证的」。
2. 改了行为，顺手更新对应文档（README / SETUP / API）。
3. 保持本项目的底线（见下），这是它值得被信任的根本。

## 不可动摇的底线（请帮我们守住）
- **单向、只增不改**：只读用户的 Get笔记；只新建/移动/追加归档文件、只新增标签；**绝不删除或修改用户原始资料**。
- **幂等**：任何脚本可反复跑不出错、不重复。
- **凭据安全**：绝不把任何 key/token 写进代码或日志；个人配置一律 `.gitignore`。
- **人在回路**：拿不准的进 staging、敏感内容只标记请人复核，不替用户做不可逆决定。
- 设计取舍的来龙去脉见 `docs/PITFALLS.md`，动手前值得一读。

## 一点说明
作者也只是一个想把自己几百条语音笔记好好沉淀下来的普通用户，把一路摸索出来的东西整理出来分享。
它不完美，也一定有更优雅的写法——你的每一个 issue、PR、甚至一句吐槽，都会让它更好。谢谢你来。
